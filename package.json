{
  "name": "sf-pipeline",
  "version": "1.0.0",
  "description": "Salesforce async data pipeline",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build:client": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "express": "^4.18.2",
    "ws": "^8.16.0"
  }
}
