{
  "id": "account-export",
  "name": "Account Export",
  "version": "1.0",
  "description": "Export and process Account records with field enrichment",
  "query": "SELECT Id, Name, CreatedDate FROM Account ORDER BY Id ASC",
  "plugins": ["LogPlugin"],
  "defaultBatchSize": 500,
  "defaultThreads": 3
}
